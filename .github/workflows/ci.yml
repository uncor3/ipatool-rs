name: ipatool-rs CI

on:
  workflow_dispatch:
  push:
    paths:
      - "**.rs"
      - "**.toml"

jobs:
  build-linux:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: "recursive"

      - name: Setup Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1

      - name: Build lib
        run: |
          cargo build

      - name: Build bin
        run: |
          #lets do auth info for now  
          #we need tests
          cargo run --features=cli auth info
